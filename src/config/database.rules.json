{
  "rules": {
    ".read": "true",
    "users": {
      "$uid": { ".write": "auth.uid == $uid" }
    },
    "projects": {
      "$uid": { ".write": "auth.uid == $uid" }
    },
    "features": {
      "$uid": { ".write": "auth.uid == $uid" }
    },
    "specs": {
      "$uid": { ".write": "auth.uid == $uid" }
    }
  }
}
